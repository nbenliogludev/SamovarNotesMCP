# Optional local development overrides only.
# The packaged DMG stores user keys locally through the app settings screen.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
NOTION_ACCESS_TOKEN=
